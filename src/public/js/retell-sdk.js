// Create a wrapper for the SDK
import { EventEmitter } from "eventemitter3";
import { Room, RoomEvent, Track, createAudioAnalyser } from "livekit-client";

export class RetellWebClient extends EventEmitter {
  // Copy the contents from your index.js here
  // ... rest of the RetellWebClient implementation
}
