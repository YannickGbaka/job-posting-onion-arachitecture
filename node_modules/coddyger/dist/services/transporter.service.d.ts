import { Kafka } from 'kafkajs';
export declare class TransporterService {
    topics: Array<any>;
    constructor(topics: Array<any>);
    kafka: Kafka;
    producer: import("kafkajs").Producer;
    consumer: import("kafkajs").Consumer;
    send(value: string, topic?: string): Promise<void>;
    get(): void;
    getData(data: any, set: any, label: string): void;
}
